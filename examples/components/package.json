{
  "main": "app.js",
  "scripts": {
    "start": "../../node_modules/watchify/bin/cmd.js . -o bundle.js -v -d",
    "build": "rm build.js && browserify app.js > bundle.js"
  },
  "browserify": {
    "transform": [
      [
        "babelify",
        {
          "stage": 0
        }
      ]
    ]
  },
  "dependencies": {}
}
